{
  "name": "ai-upskill-triage-harness",
  "private": true,
  "version": "1.0.0",
  "description": "Breakable Node + Playwright app for exercising an AI test failure triage agent in CI",
  "scripts": {
    "start": "node app/server.mjs",
    "test": "playwright test",
    "condense": "node scripts/condense-report.mjs",
    "break": "node scripts/apply-defect.mjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
